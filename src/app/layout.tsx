import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
});

export const metadata: Metadata = {
  title: {
    template: "%s - InstaBook",
    default: "InstaBook",
  },
  description:
    "Welcome to InstaBook, the social media platform designed to bring your moments to life. Share your photos, connect with friends, and explore new experiences with ease. With InstaBook, every post becomes a cherished memory, and every connection a meaningful relationship. Join us to capture, share, and celebrate life's best moments, all in one place.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        {children}
      </body>
    </html>
  );
}
